// https://projecteuler.net/problem=1

sumMultiples: function integer (n: integer) = {
    sum: integer;
    for (i: integer; i < 1000; i++) {
        if (i % 3 == 0 || i % 5 == 0) {
            sum = sum + i;
        }
    }
    return sum;
}

print sumMultiples(1000);
